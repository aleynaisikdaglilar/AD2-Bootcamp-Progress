<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/home_imageView"
        android:layout_width="231dp"
        android:layout_height="244dp"
        android:layout_marginTop="76dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_home_image" />

    <TextView
        android:id="@+id/learnAtHome_textView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="44dp"
        android:text="Learn at home!"
        app:layout_constraintEnd_toEndOf="@+id/home_imageView"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/home_imageView"
        app:layout_constraintTop_toBottomOf="@+id/home_imageView"
        style="@style/MainTitleStyle" />

    <TextView
        android:id="@+id/sub_textView"
        android:text="efficiently and effectively"
        app:layout_constraintEnd_toEndOf="@+id/learnAtHome_textView"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="@+id/learnAtHome_textView"
        app:layout_constraintTop_toBottomOf="@+id/learnAtHome_textView"
        style="@style/SubTitleStyle" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/startNow_button"
        style="@style/bigButton"
        android:text="@string/start_now"
        app:cornerRadius="8dp"
        app:layout_constraintEnd_toEndOf="@+id/sub_textView"
        app:layout_constraintStart_toStartOf="@+id/sub_textView"
        app:layout_constraintTop_toBottomOf="@+id/sub_textView"
        />
</androidx.constraintlayout.widget.ConstraintLayout>
</layout>